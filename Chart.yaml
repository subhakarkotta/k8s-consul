apiVersion: v2
name: consul
description: A Helm chart for Kubernetes
type: application
version: "0.1.0"
appVersion: "1.16.0"
dependencies:
  - name: consul
    version: "1.3.3"
    repository: "https://helm.releases.hashicorp.com"
